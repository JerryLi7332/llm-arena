# ========================================
# LLM Arena 项目环境配置文件
# ========================================
# 1. 此文件包含敏感信息，切勿提交到 Git
# 2. 已在 .gitignore 中配置忽略
# 3. 修改配置后需要重启应用
# ========================================

# ========================================
# 🐍 Python 环境配置
# ========================================
PYTHONPATH=src

# ========================================
# 📦 应用基础配置
# ========================================
VERSION=1.0.0
APP_TITLE=LLM Arena - AI对战平台
PROJECT_NAME=llm-arena
APP_DESCRIPTION=基于大语言模型的策略游戏对战平台

# 运行环境：development | production | testing
APP_ENV=development

# 调试模式（生产环境必须设置为 False）
DEBUG=True

# ========================================
# 🔐 安全配置
# ========================================
# JWT 加密密钥（使用 python -c "import secrets; print(secrets.token_hex(32))" 生成）
SECRET_KEY=a8f4e7d9c2b1a3f6e8d4c9b7a5f3e1d8c6b4a2f0e9d7c5b3a1f8e6d4c2b0a9f7

# Swagger API 文档访问凭证
SWAGGER_UI_USERNAME=admin
SWAGGER_UI_PASSWORD=llm_arena_2025

# ========================================
# 🎫 JWT 认证配置
# ========================================
JWT_ALGORITHM=HS256
# Token 过期时间（分钟）：1440=1天, 10080=7天, 43200=30天
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ========================================
# 💾 数据库配置
# ========================================
# 数据库引擎：sqlite | postgresql
DB_ENGINE=sqlite

# SQLite 配置（开发环境推荐）
# DB_ENGINE=sqlite 时，数据会存储在 ./db.sqlite3 文件中

# PostgreSQL 配置（生产环境推荐）
# DB_ENGINE=postgresql 时，需要配置以下参数：
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_postgres_password
DB_NAME=llm_arena

# ========================================
# 🌐 CORS 跨域配置
# ========================================
# 允许访问的前端地址（多个地址用逗号分隔，支持通配符 *）
CORS_ORIGINS=http://localhost:*,http://127.0.0.1:*,http://localhost:5173,http://localhost:3000

# ========================================
# 🤖 DeepSeek API 配置
# ========================================
# 从 https://platform.deepseek.com/ 获取 API Key
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here

# API 服务器地址（通常不需要修改）
DEEPSEEK_API_BASE=https://api.deepseek.com/v1

# 默认使用的模型
# 可选：deepseek-chat | deepseek-coder
DEEPSEEK_DEFAULT_MODEL=deepseek-chat

# 单次请求最大生成 token 数（1-8000）
DEEPSEEK_MAX_TOKENS=4000

# 生成温度（0.0-2.0）
# 0.0-0.3: 更确定、一致
# 0.4-0.7: 平衡创造性和一致性（推荐）
# 0.8-2.0: 更有创造性、随机性
DEEPSEEK_TEMPERATURE=0.7

# 请求超时时间（毫秒）
DEEPSEEK_REQUEST_TIMEOUT=30000

# API 调用速率限制
DEEPSEEK_RATE_LIMIT_PER_MINUTE=60
DEEPSEEK_RATE_LIMIT_PER_HOUR=1000

# 费用限制（美元）
DEEPSEEK_MAX_COST_PER_REQUEST=0.01
DEEPSEEK_MAX_COST_PER_DAY=1.0

# ========================================
# 📁 文件存储配置
# ========================================
# 用户上传的 AI 代码存储路径
UPLOAD_DIR=./uploads

# 允许上传的文件扩展名
ALLOWED_EXTENSIONS=.py,.txt,.md,.json

# 单个文件最大大小（MB）
MAX_FILE_SIZE=10

# ========================================
# 📊 Redis 缓存配置（可选）
# ========================================
# 如果不使用 Redis，可以注释掉这些配置
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0

# ========================================
# 📝 日志配置
# ========================================
LOG_LEVEL=INFO
LOG_FILE=./logs/app.log

# ========================================
# 🎮 游戏配置
# ========================================
# 游戏对战超时时间（秒）
GAME_TIMEOUT=300

# 每个用户同时进行的最大游戏数
MAX_CONCURRENT_GAMES=5

# ELO 初始分数
INITIAL_ELO_SCORE=1200

# ========================================
# 📧 邮件配置（可选）
# ========================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASSWORD=your_app_password
# SMTP_FROM=noreply@llm-arena.com

# ========================================
# 🔔 通知配置（可选）
# ========================================
# ENABLE_EMAIL_NOTIFICATIONS=False
# ENABLE_WEBHOOK_NOTIFICATIONS=False

# ========================================
# 📈 监控配置（可选）
# ========================================
# ENABLE_METRICS=True
# METRICS_PORT=9090
